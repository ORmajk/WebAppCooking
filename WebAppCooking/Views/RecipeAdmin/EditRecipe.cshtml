@model WebAppCooking.Models.Recipe

@{
    ViewData["Title"] = "Редактирование рецепта";
}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/StyleEditRecipe.css" />
</head>
<body style="background-color: #FFAA00">
    <div class="auth-wrapper">
        <div class="auth-container" style="max-width: 800px;">
            <div class="auth-card">
                <div class="auth-header">
                    <h4 class="auth-title">Редактирование рецепта</h4>
                </div>
                <div class="auth-body">
                    <form asp-action="EditRecipe" method="post">
                        <input type="hidden" asp-for="IdRecipe" />
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="form-group">
                            <label asp-for="RecipeName" class="control-label">Название рецепта</label>
                            <input asp-for="RecipeName" class="form-control" />
                            <span asp-validation-for="RecipeName" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="RecipeDescription" class="control-label">Описание</label>
                            <textarea asp-for="RecipeDescription" class="form-control" rows="3"></textarea>
                            <span asp-validation-for="RecipeDescription" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="RecipeImage" class="control-label">Изображение</label>
                            <input asp-for="RecipeImage" class="form-control" />
                            <span asp-validation-for="RecipeImage" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="RecipeDate" class="control-label">Дата</label>
                            <input asp-for="RecipeDate" class="form-control" type="date" />
                            <span asp-validation-for="RecipeDate" class="text-danger"></span>
                        </div>

                        <div class="form-group">
                            <label asp-for="IdRecipeType" class="control-label">Тип рецепта</label>
                            <select asp-for="IdRecipeType" class="form-control" asp-items="ViewBag.IdRecipeType">
                                <option value="">Выберите тип</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label asp-for="IdAuthor" class="control-label">Автор</label>
                            <select asp-for="IdAuthor" class="form-control" asp-items="ViewBag.IdAuthor">
                                <option value="">Выберите автора</option>
                            </select>
                        </div>

                        <div class="form-group">
                            <label class="control-label">Ингредиенты</label>
                            <select name="selectedIngredients" class="form-control" multiple asp-items="ViewBag.Ingredients"></select>
                            <small class="form-text text-muted">Удерживайте Ctrl для выбора нескольких ингредиентов</small>
                        </div>

                        <div class="form-actions">
                            <button type="submit" class="btn-save">Сохранить</button>
                            <a asp-action="Index" class="btn-cancel">Отмена</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
